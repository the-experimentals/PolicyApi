apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "policyapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "policyapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "policyapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
